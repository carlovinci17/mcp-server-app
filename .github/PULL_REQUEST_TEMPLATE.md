## What changed

## Why

## Testing

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` passes
- [ ] `uv run black --check .` passes
- [ ] Manually verified against a running `func start` (and `swa start` if the frontend is affected)
